import { HittingStatisticsByPlayer } from './hitting-statistics-by-player';

describe('HittingStatisticsByPlayer', () => {
  it('should create an instance', () => {
    expect(new HittingStatisticsByPlayer()).toBeTruthy();
  });
});
