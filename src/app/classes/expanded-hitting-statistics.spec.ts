import { ExpandedHittingStatistics } from './expanded-hitting-statistics';

describe('ExpandedHittingStatistics', () => {
  it('should create an instance', () => {
    expect(new ExpandedHittingStatistics()).toBeTruthy();
  });
});
