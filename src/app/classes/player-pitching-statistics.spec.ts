import { PlayerPitchingStatistics } from './player-pitching-statistics';

describe('PlayerPitchingStatistics', () => {
  it('should create an instance', () => {
    expect(new PlayerPitchingStatistics()).toBeTruthy();
  });
});
