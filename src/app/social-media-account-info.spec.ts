import { SocialMediaAccountInfo } from './social-media-account-info';

describe('SocialMediaAccountInfo', () => {
  it('should create an instance', () => {
    expect(new SocialMediaAccountInfo()).toBeTruthy();
  });
});
